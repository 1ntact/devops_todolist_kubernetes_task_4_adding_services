apiVersion: v1
kind: Service
metadata:
 name: todoapp-service
 namespace: todoapp
spec:
  type: ClusterIP
  selector:
    app: todolist
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
