apiVersion: v1
kind: Service
metadata:
 name: todoapp-nodeport-service
 namespace: todoapp
 labels:
   app: todolist
spec:
  type: NodePort
  selector:
    app: todolist
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007
      protocol: TCP

